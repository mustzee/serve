# Claude와 유사한 모델 사용 설정
# Qwen2.5 사용 (가장 Claude와 비슷)

host: "0.0.0.0"
port: 8000
workers: 1

# Ollama 백엔드 사용
backend_type: "ollama"
default_model: "qwen2.5:7b"  # Claude와 가장 유사

ollama_base_url: "http://localhost:11434"

model_cache_dir: "./model_cache"

cors_origins:
  - "*"
